<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ezen.dog.cart.Cservice">
<insert id="addcart">
	insert into cart (cart_id, userId, product_id, CART_QUANTITY, cart_date)
	values (cart_seq.nextval, #{param1}, #{param2},#{param3}, sysdate)
</insert>
<select id="cartout" resultType="com.ezen.dog.cart.CartDTO">
	select * from cart where userId=#{param1}
</select>


</mapper>